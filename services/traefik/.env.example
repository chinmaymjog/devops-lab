# ==============================================================================
# Traefik Configuration Template
# ==============================================================================

# 📦 Image Version
TRAEFIK_TAG=v3.3

# 🌐 Domain Setup
# The root domain used for all routed services (e.g., example.com)
# For local development, 'lab.local' is recommended.
APP_DOMAIN=lab.local

# ☁️ Cloudflare Integration (for DNS-01 Challenge)
# Your Cloudflare account email
CF_API_EMAIL=your-email@example.com

# Cloudflare DNS API Token
# Generate at: https://dash.cloudflare.com/profile/api-tokens
# Required Permissions: Zone.DNS (Edit), Zone.Zone (Read)
CF_DNS_API_TOKEN=your-cloudflare-api-token

# 🔐 Security
# Basic Authentication for the Traefik Dashboard (format: username:hashed_password)
# Generate with htpasswd: echo $(htpasswd -nbu user password) | sed -e s/\\$/\\$\\$/g
BASIC_AUTH=admin:$$2y$$05$$example_hash_replace_me
