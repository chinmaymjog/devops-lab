# ==============================================================================
# Prometheus Remote Environment Template (env.sample)
# ==============================================================================
# This file is used by CI/CD to inject secrets via 'envsubst'.

# 📦 Image Version
PROMETHEUS_TAG=$PROMETHEUS_TAG

# 🌐 Networking
# The domain Prometheus will be accessible at (e.g., prometheus.lab.local)
APP_DOMAIN=$APP_DOMAIN
