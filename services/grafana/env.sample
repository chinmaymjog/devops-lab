# ==============================================================================
# Grafana Remote Environment Template (env.sample)
# ==============================================================================
# This file is used by CI/CD to inject secrets via 'envsubst'.

# 📦 Image Version
GRAFANA_TAG=$GRAFANA_TAG

# 🌐 Networking
# The domain Grafana will be accessible at (e.g., grafana.lab.local)
APP_DOMAIN=$APP_DOMAIN

# 🔑 Credentials
# Default Admin User: admin
# Default Admin Password: admin (you will be prompted to change this on first login)
