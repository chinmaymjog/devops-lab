# ==============================================================================
# PostgreSQL Configuration Template
# ==============================================================================

# 📦 Image Version
POSTGRES_TAG=17.6

# 🌐 Networking (Internal to Traefik)
APP_DOMAIN=lab.local

# 🔑 Credentials
# Primary password for the 'postgres' superuser
POSTGRES_PASSWORD=your-secure-password

# Default database to create on startup (optional)
POSTGRES_DB=myapp_db

# Default administrative user (optional, defaults to 'postgres')
POSTGRES_USER=postgres
